# can_driver 测试计划

日期：2026-03-03
状态：部分完成

## 1. 已完成的测试 ✅

### 1.1 test_safe_command.cpp（9 个测试）
- ✅ 正常值转换
- ✅ 上溢出保护
- ✅ 下溢出保护
- ✅ NaN 拒绝
- ✅ Infinity 拒绝
- ✅ 零 scale 拒绝
- ✅ 负 scale 拒绝
- ✅ 极小 scale 边界情况
- ✅ int16 钳制

**覆盖率：完整** ⭐⭐⭐⭐⭐

### 1.2 test_joint_config_parser.cpp（5 个测试）
- ✅ 整数 motor_id 解析
- ✅ 十六进制字符串 motor_id 解析
- ✅ 负数 motor_id 拒绝
- ✅ 溢出 motor_id 拒绝
- ✅ 非法字符串拒绝

**覆盖率：良好** ⭐⭐⭐⭐

### 1.3 test_socketcan_controller.cpp（3 个测试）
- ✅ CAN 帧编码/解码往返测试
- ✅ 接收处理器生命周期
- ✅ shutdown 重置状态

**覆盖率：基础** ⭐⭐⭐

### 1.4 test_device_manager.cpp（9 个测试）
- ✅ ensureTransport 创建设备
- ✅ ensureTransport 幂等性
- ✅ getTransport 不存在返回 null
- ✅ getProtocol 未初始化返回 null
- ✅ ensureProtocol 创建协议
- ✅ ensureProtocol 无 transport 失败
- ✅ getDeviceMutex 不存在返回 null
- ✅ getDeviceMutex 正常返回
- ✅ shutdownAll 清空设备

**覆盖率：良好** ⭐⭐⭐⭐

### 1.5 test_limits_enforcement.cpp（7 个测试）
- ✅ 正向限位执行
- ✅ 负向限位执行
- ✅ 范围内值透传
- ✅ 速度限位对称性
- ✅ 缩放与钳制组合
- ✅ 溢出保护
- ✅ direct 命令与 ros_control 命令限位一致性

**覆盖率：数学逻辑完整** ⭐⭐⭐⭐

**总计：33 个单元测试，全部通过** ✅

## 2. 缺失的关键测试 ⚠️

### 2.1 CanDriverHW 集成测试（高优先级）

**需要测试的场景：**

1. **初始化流程测试**
   - 正常初始化
   - 参数缺失时的失败处理
   - 重复初始化的幂等性

2. **read/write 循环测试**
   - 正常的 read -> write 循环
   - active_ 为 false 时的早期返回
   - 设备不可用时的降级处理

3. **限位端到端测试**
   - URDF 限位是否真的生效
   - rosparam 覆盖是否工作
   - 无限位时的警告

4. **direct 命令超时测试**
   - direct 命令在超时前生效
   - 超时后自动回退到 ros_control 命令
   - 超时参数可配置

**实现建议：**
```cpp
// tests/test_can_driver_hw_integration.cpp
// 使用 mock transport 或 loopback 模式
// 避免需要真实 CAN 硬件
```

### 2.2 并发安全测试（中优先级）

**需要测试的场景：**

1. **多线程读写测试**
   - 同时调用 read() 和 write()
   - 同时调用 service 和 read/write
   - 验证没有数据竞争

2. **锁顺序测试**
   - 验证不会死锁
   - 压力测试（1000+ 次并发调用）

**实现建议：**
```cpp
// tests/test_concurrency.cpp
// 使用 std::thread 创建多个线程
// 使用 ThreadSanitizer 检测数据竞争
```

### 2.3 错误路径测试（中优先级）

**需要测试的场景：**

1. **设备故障模拟**
   - CAN 设备突然断开
   - 协议层返回错误
   - 异常抛出时的处理

2. **配置错误测试**
   - 非法的 protocol 字符串
   - 非法的 control_mode
   - scale 为 0 或负数

**实现建议：**
```cpp
// tests/test_error_handling.cpp
// 使用 mock 对象模拟故障
```

### 2.4 性能测试（低优先级）

**需要测试的场景：**

1. **实时性能测试**
   - read/write 循环延迟
   - 1kHz 控制频率下的稳定性
   - 内存分配检测（不应有堆分配）

2. **资源泄漏测试**
   - 长时间运行无内存泄漏
   - 文件描述符不泄漏

**实现建议：**
```bash
# 使用 valgrind 检测内存泄漏
valgrind --leak-check=full ./can_driver_node

# 使用 perf 分析性能
perf record -g ./can_driver_node
```

## 3. 上真机前的检查清单 🚨

### 3.1 必须完成（阻塞项）

- [ ] **运行所有单元测试** ✅（已完成）
- [ ] **检查 URDF 限位配置**
  ```bash
  rosrun can_driver can_driver_node
  # 查看日志，确保每个 joint 都有 "URDF limits" 或 "rosparam overrides limits"
  # 如果看到 "no limits found"，必须修复！
  ```
- [ ] **软件回环模式测试**
  ```bash
  # 编译时定义 SOFTWARE_LOOPBACK_MODE=1
  # 运行完整的控制循环，确保无崩溃
  ```
- [ ] **CAN 总线监控**
  ```bash
  candump can0
  # 发送小幅度命令，观察 CAN 帧是否合理
  ```

### 3.2 强烈建议

- [ ] **补充 CanDriverHW 集成测试**
- [ ] **补充 direct 命令超时测试**
- [ ] **准备急停按钮**
- [ ] **录屏 + 日志记录**

### 3.3 渐进式上电流程

1. **阶段 1：无电机测试**
   - 只测 CAN 通信
   - 验证帧格式正确

2. **阶段 2：单电机小幅度**
   - 接一个电机
   - 发送 0.01 rad 命令
   - 观察 5 分钟

3. **阶段 3：逐步增加**
   - 每次增加 10% 幅度
   - 确认无异常后继续

4. **阶段 4：全系统测试**
   - 所有电机同时运行
   - 长时间稳定性测试

## 4. CI/CD 集成

### 4.1 当前状态
- ✅ 有 `.github/workflows/ci.yml`
- ⚠️ 需要确保 CI 运行所有测试

### 4.2 建议的 CI 流程
```yaml
- name: Run tests
  run: |
    source devel/setup.bash
    catkin_make run_tests_can_driver
    catkin_test_results
```

## 5. 测试覆盖率目标

| 模块 | 当前覆盖率 | 目标覆盖率 |
|------|-----------|-----------|
| SafeCommand | 100% | 100% ✅ |
| JointConfigParser | 80% | 90% |
| DeviceManager | 70% | 85% |
| SocketCanController | 40% | 70% |
| CanDriverHW | 0% | 60% |

## 6. 下一步行动

### 立即执行（本周）
1. 补充 CanDriverHW 集成测试
2. 补充 direct 命令超时测试
3. 验证 URDF 限位配置

### 短期（下周）
1. 补充并发安全测试
2. 补充错误路径测试
3. 完善 CI 流程

### 长期（下月）
1. 性能测试与优化
2. 压力测试
3. 长时间稳定性测试

## 7. 风险评估

| 风险 | 严重性 | 可能性 | 缓解措施 |
|------|--------|--------|----------|
| 限位配置错误导致电机超限 | 高 | 中 | 上真机前必须验证限位日志 |
| 并发 bug 导致数据竞争 | 中 | 低 | 补充并发测试 |
| 设备故障时崩溃 | 中 | 中 | 补充错误路径测试 |
| 性能不满足实时要求 | 低 | 低 | 性能测试与优化 |

## 8. 总结

**当前状态：可以谨慎上真机，但需要严格遵循检查清单**

- 单元测试覆盖率：良好（33 个测试全部通过）
- 集成测试覆盖率：不足（需要补充）
- 代码质量：优秀（模块化、文档完善）
- 上真机风险：中等（需要渐进式测试）

**建议：先在测试台上验证，不要直接上主力机器人。**
