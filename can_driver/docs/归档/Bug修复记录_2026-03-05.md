# Bug修复记录（整理自评审资料）

日期：2026-03-05  
范围：`can_driver` 包内 `MtCan` / `EyouCan` 协议实现  
来源：用户提供的两批问题清单（MtCan 一轮修复说明 + EyouCan 协议对照审查）

## MtCan 修复记录

| 编号 | 严重度 | 问题摘要 | 修复方案 | 当前状态 |
|---|---|---|---|---|
| #1 | 🔴 致命 | `handleResponse` 用 `canId & 0xFF` 提取 ID，导致 `0x241 -> 0x41` 错误映射 | 改为 `nodeId = canId - kResponseBaseId` | 已修复 |
| #2 | 🔴 | `requestState/requestError` 使用 `dlc=1`，且尾字节未清零 | 改为 `dlc=8` + `data.fill(0)` | 已修复 |
| #3 | 🔴 | `Disable` 用 `0x81(Stop)`，未真正关闭输出 | 改为 `0x80(Motor Off)` | 已修复 |
| #4 | 🟠 | `Enable` 每次触发 `setZeroPosition` + `resetSystem` | 移除自动写零点，仅更新本地状态 | 已修复 |
| #5 | 🟠 | `getPosition` 返回命令值而非实际位置 | 新增 `0x92` 轮询，返回 `multiTurnAngle` | 已修复 |
| #6 | 🟡 | `0x9C` 未完整解析温度/编码器；运动应答未复用状态解析 | 增加温度、编码器字段并补全 case | 已修复 |
| #7 | 🟡 | `getVelocity` 以 `velocity==0` 作为刷新条件，误判静止态 | 移除该判断，仅在无状态时请求 | 已修复 |
| #8 | 🟢 | 电流值 double 往返转换冗余 | 保留兼容实现，后续可优化 | 已知待优化 |
| + | 🟡 | `setPosition` 的 `maxSpeed` 符号处理风险 | 使用绝对值并限制到 `uint16_t` | 已修复 |
| + | 🟢 | 非本驱动帧日志洪泛 | 非匹配帧静默忽略 | 已修复 |

### MtCan 关键验证点（建议回归）

1. 发送 `requestState(1)`，确认 `0x241` 响应更新 `motorStates[1]`。  
2. 调用 `getCurrent(1)/getVelocity(1)`，确认能回读非零状态。  
3. `Disable(1)` 后验证发送命令为 `0x80`。  
4. 连续 `Enable/Disable` 循环，确认不会反复写零点和复位。  
5. 运动过程中连续读取 `getPosition`，确认是实际位置变化而非目标值瞬时跳变。

## EyouCan 审查记录

| 编号 | 严重度 | 问题摘要 | 修复方案 | 当前状态 |
|---|---|---|---|---|
| #1 | 🔴 | `Stop()` 下发 `0x11=0x00`，与协议不符 | 改为发送 `0x11=0x01` | 已修复 |
| #2 | 🔴 | 写 ACK（`0x02`）把 `STATE` 当寄存器值 | 改为仅判定写入结果码并处理失败回滚 | 已修复 |
| #3 | 🔴 | ACK 分支读取 `data[5]` 越界语义错误 | 移除错误取值逻辑 | 已修复 |
| #4 | 🔴 | `0x15` 告警读取偏移错误（从 4 读） | 改为从 `data[2]` 读 32 位码 | 已修复 |
| #5 | 🟠 | `setAcceleration` 未下发协议命令 | 增加 `0x0B` 写入 | 已修复 |
| #6 | 🟠 | `setDeceleration` 未下发协议命令 | 增加 `0x0C` 写入 | 已修复 |
| #7 | 🟡 | `getCurrent` 未读 `0x05` | 增加 `requestCurrent` + 缓存返回 | 已修复 |
| #8 | 🟡 | `getVelocity` 返回命令值非实际速度 | 改为读 `0x06` 实际速度并缓存 | 已修复 |
| #9 | 🟢 | `position==0` 作为未刷新判定不可靠 | 改为 `positionReceived` 标志位 | 已修复 |
| #10 | 🟢 | 帧长度可变导致固件兼容风险 | 读写统一 `DLC=8` 并零填充 | 已修复 |

## 头文件同步记录

为匹配最新实现，已补充 `MtCan.h`：

1. `MotorState` 新增 `multiTurnAngle/temperature/encoderPosition` 字段。  
2. 新增私有方法声明 `requestMultiTurnAngle(uint8_t)`。  
3. 更新 `Enable/Disable` 注释语义（`Enable` 不再隐式写零点，`Disable` 为 `Motor Off`）。

---

备注：本记录为“问题-修复-状态”归档文档，便于后续测试计划和发布说明直接引用。

## 第二轮修复日志（同日补充）

时间：2026-03-05  
提交：`138d1a2`  
目的：修复共享总线过滤、位置位宽、默认速度与刷新负载问题，并补充回归测试。

| 编号 | 模块 | 问题摘要 | 修复动作 | 状态 |
|---|---|---|---|---|
| R2-1 | EyouCan | 共享总线下对非本协议帧打印错误日志 | `handleResponse` 对非 Eyou ID 范围静默丢弃，不再逐帧 `cerr` | 已修复 |
| R2-2 | EyouCan | 对 `0x00-0xFF` 任意帧缺少受管电机过滤 | 增加 `managedMotorIds` 白名单，仅处理本驱动管理 ID | 已修复 |
| R2-3 | MtCan/CanProtocol | `getPosition()` 存在 `int64 -> int32` 截断 | 接口统一为 `int64_t getPosition()`，返回多圈角度原值 | 已修复 |
| R2-4 | MtCan | 未先设速度时 `setPosition` 发送 `maxSpeed=0` | 速度为 0 时使用默认 `100 dps` | 已修复 |
| R2-5 | EyouCan | 写失败时本地状态一致性风险 | 去除写前乐观更新；ACK 失败后触发寄存器重读同步 | 已修复 |
| R2-6 | SocketCanController | 帧转换尾字节显式清零不足 | `to/fromSocketCanFrame` 统一 `data.fill(0)` 后拷贝 DLC | 已修复 |
| R2-7 | EyouCan/MtCan | 刷新线程固定 1ms，负载风险高 | 按电机数动态退让刷新间隔（不再固定 1ms） | 已修复 |

### 第二轮验证结果

1. `catkin_make --pkg can_driver` 编译通过。  
2. `test_mt_can_protocol` 通过（新增默认速度与大角度位置回归）。  
3. `test_eyou_can_protocol` 通过（新增非本协议/非受管 ID 过滤回归）。

## 第三轮修复日志（同日补充）

时间：2026-03-05  
目的：修复成立项中的可落地工程问题（不含协议魔数重构）。

| 编号 | 模块 | 问题摘要 | 修复动作 | 状态 |
|---|---|---|---|---|
| R3-1 | SocketCanController | 无效帧日志信息不足 | 增加 `id/dlc/ext/rtr/err` 明细字段，便于现场定位 | 已修复 |
| R3-2 | SocketCanController | `dispatchReceive` 拷贝整张 handlers map 开销偏大 | 改为只快照 `ReceiveHandler` 列表，避免复制 key/hash 桶结构 | 已修复 |
| R3-3 | CanDriverHW | YAML 未拦截 `motor_id` 重复配置 | 在 `parseAndSetupJoints` 增加全局 `motor_id` 唯一性校验并报错 | 已修复 |
| R3-4 | CanDriverHW | `recover` 失败信息过于笼统 | 按 `MotorOpStatus` 返回细化错误信息（设备未就绪/协议不可用/拒绝/执行失败） | 已修复 |
| R3-5 | CanDriverHW | YAML 未拦截重复 `joint` 名称 | 增加 `joint name` 唯一性校验并报错 | 已修复 |

### 第三轮验证结果

1. `catkin_make --pkg can_driver` 编译通过。

## 第四轮修复日志（同日补充）

时间：2026-03-05  
目的：修复评审结论中的 1/2/3 项可用性问题（配置校验、PP 位宽安全、状态字段发布）。

| 编号 | 模块 | 问题摘要 | 修复动作 | 状态 |
|---|---|---|---|---|
| R4-1 | JointConfigParser | `control_mode` 未校验，拼写错误会静默落到 position 分支 | 在解析阶段强校验 `control_mode` 必须为 `velocity/position`，否则启动失败并报错 | 已修复 |
| R4-2 | EyouCan | PP 电流/速度 32 位值直接强转 `int16_t`，存在回绕截断风险 | 返回前增加有界钳制（`int16` 范围）并 `ROS_WARN_THROTTLE` 告警 | 已修复 |
| R4-3 | CanDriverHW + 协议层 | `motor_states` 的 `enabled/fault` 字段未填充，语义失真 | 扩展 `CanProtocol` 状态查询接口；`MtCan/EyouCan` 提供实现；`publishMotorStates` 按设备批量填充 `enabled/fault` | 已修复 |

### 第四轮测试补充

1. `test_joint_config_parser` 新增非法 `control_mode` 拒绝用例。  
2. `test_eyou_can_protocol` 新增电流/速度越界钳制用例。  
3. `test_mt_can_protocol` 新增 `enabled/fault` 可观测性用例。
