# 细化开发步骤与检查清单

## 口径统一（2026-03-10）

本节用于覆盖旧段落中与当前代码状态不一致的描述，后文历史清单仅作过程记录。

### 当前已完成（以代码与提交为准）

- `arm_traj_splitter`：
  - 容差拆分支持 unnamed 容差按索引回退
  - 关节索引构建已改为哈希查表
- `robot_bringup`：
  - `acceptance_smoke.launch` 已支持可选自动图检查（`enable_graph_check`）
- `yiyou_canopen`：
  - `canopen_motor_node` 已增加 `respawn`、`respawn_delay`
  - `controller_spawner` 已增加超时参数
  - `package.xml` 已补运行依赖 `controller_manager`
  - 转换因子来源已文档化（`yiyou_canopen/docs/12_canopen参数说明.md`）
- `car_total`：
  - `display.launch` 已移除对不存在 `urdf.rviz` 的硬依赖

### 本轮排除项

- `robot_moveit_config`（MoveIt 完整配置生成与验证）
- `can_driver_ui`（测试 UI 健壮性改造）

### 下一轮入口文档

- `docs/11_按commit修复指南.md`

## 当前状态分析

根据系统提示，以下配置已经完成：

### ✅ 已完成的配置

1. **can_driver 侧**：
   - [can_driver.yaml](../can_driver/config/can_driver.yaml)：关节命名已更新为 arm_joint_1~4 + left_track + right_track
   - [ros_controllers.yaml](../can_driver/config/ros_controllers.yaml)：pp_arm_controller + diff_drive_controller 已配置
   - [can_driver.launch](../can_driver/launch/can_driver.launch)：joint_states remap 已添加

2. **yiyou_canopen 侧**：
   - [motor.yaml](../yiyou_canopen/config/motor.yaml)：关节命名已更新为 arm_joint_5~6 + flipper_1~4
   - [yiyou_canopen.launch](../yiyou_canopen/launch/yiyou_canopen.launch)：命名空间、controllers 加载、joint_states remap 已配置

### 🔄 待完成的工作

- 阶段一：URDF 验证（需要找到或创建 URDF）
- 阶段二：Controller 验证（需要创建 controllers.yaml for canopen）
- 阶段三：joint_states 合并节点
- 阶段四：arm_traj_splitter 代理节点（核心开发）
- 阶段五：MoveIt 配置
- 阶段六：底盘与摆臂测试
- 阶段七：顶层 Launch 整合

---

## 阶段一：URDF 验证与完善（预计 4 小时）

### 1.1 查找或创建 URDF

**任务**：确认机器人 URDF 文件是否存在

```bash
# 查找 URDF 文件
find ~/Robot24_catkin_ws/src -name "*.urdf" -o -name "*.xacro" | grep -v realsense
```

**检查清单**：
- [ ] 找到机器人主 URDF/xacro 文件
- [ ] 如果不存在，需要创建 robot_description 包

### 1.2 验证关节命名

**任务**：确保 URDF 中的 12 个关节名称与配置文件一致

**检查清单**：
- [ ] arm_joint_1 ~ arm_joint_6（机械臂 6 轴）
- [ ] left_track, right_track（底�� 2 轮）
- [ ] flipper_1 ~ flipper_4（摆臂 4 个）

**验证命令**：
```bash
# 检查 URDF 语法
check_urdf robot.urdf

# 查看关节列表
grep '<joint name=' robot.urdf
```

### 1.3 填写关节限位

**任务**：为每个关节设置合理的限位参数

**URDF 示例**：
```xml
<joint name="arm_joint_1" type="revolute">
  <parent link="base_link"/>
  <child link="link1"/>
  <origin xyz="0 0 0" rpy="0 0 0"/>
  <axis xyz="0 0 1"/>
  <limit lower="-3.14" upper="3.14" effort="100" velocity="2.0"/>
</joint>
```

**检查清单**：
- [ ] 每个 revolute joint 有 lower/upper 限位
- [ ] 每个 joint 有 effort 和 velocity 限制
- [ ] continuous joint（如底盘轮）不需要 lower/upper

### 1.4 测试 URDF 加载

**验证命令**：
```bash
# 加载 URDF 到参数服务器
roslaunch robot_description display.launch

# 在 RViz 中查看模型
# 手动拖动关节，验证运动学正确
```

**检查清单**：
- [ ] URDF 能正常解析，无错误
- [ ] RViz 中能看到完整模型
- [ ] 手动拖动关节，运动合理

---

## 阶段二：Controller 配置验证（预计 2 小时）

### 2.1 创建 canopen 侧 controllers.yaml

**任务**：为 yiyou_canopen 创建 controllers 配置文件

**文件路径**：`~/Robot24_catkin_ws/src/yiyou_canopen/config/controllers.yaml`

**内容**：
```yaml
joint_state_controller:
  type: joint_state_controller/JointStateController
  publish_rate: 100

canopen_arm_controller:
  type: position_controllers/JointTrajectoryController
  joints:
    - arm_joint_5
    - arm_joint_6
  constraints:
    goal_time: 5.0
    arm_joint_5: {goal: 0.02}
    arm_joint_6: {goal: 0.02}

flipper_controller:
  type: position_controllers/JointGroupPositionController
  joints:
    - flipper_1
    - flipper_2
    - flipper_3
    - flipper_4
```

**检查清单**：
- [ ] 文件已创建
- [ ] yiyou_canopen.launch 中已加载（已完成）

### 2.2 验证 can_driver 侧 Controller

**启动测试**：
```bash
# 启动 can_driver
roslaunch can_driver can_driver.launch

# 检查 controller 是否加载成功
rosservice call /can_driver_node/controller_manager/list_controllers

# 检查 action server 是否存在
rostopic list | grep follow_joint_trajectory
# 应该看到：/pp_arm_controller/follow_joint_trajectory/...
```

**检查清单**：
- [ ] joint_state_controller 状态为 running
- [ ] pp_arm_controller 状态为 running
- [ ] diff_drive_controller 状态为 running
- [ ] /can_driver/joint_states 正在发布（6 个关节）

### 2.3 验证 canopen 侧 Controller

**启动测试**：
```bash
# 启动 canopen
roslaunch yiyou_canopen yiyou_canopen.launch

# 检查 controller 是否加载成功
rosservice call /canopen/controller_manager/list_controllers

# 检查 action server
rostopic list | grep canopen
```

**检查清单**：
- [ ] joint_state_controller 状态为 running
- [ ] canopen_arm_controller 状态为 running
- [ ] flipper_controller 状态为 running
- [ ] /canopen/joint_states 正在发布（6 个关节）

---

## 阶段三：joint_states 合并（预计 1 小时）

### 3.1 创建顶层 bringup 包

**任务**：创建用于系统整合的 bringup 包

```bash
cd ~/Robot24_catkin_ws/src
catkin_create_pkg robot_bringup roslaunch
```

**检查清单**：
- [ ] 包已创建
- [ ] package.xml 依赖正确

### 3.2 创建 joint_states 合并 launch

**文件路径**：`~/Robot24_catkin_ws/src/robot_bringup/launch/joint_states_merge.launch`

**内容**：
```xml
<launch>
  <!-- 合并两个驱动节点的 joint_states -->
  <node name="joint_state_publisher"
        pkg="joint_state_publisher"
        type="joint_state_publisher">
    <rosparam param="source_list">
      - can_driver/joint_states
      - canopen/joint_states
    </rosparam>
  </node>

  <!-- 发布 TF 树 -->
  <node name="robot_state_publisher"
        pkg="robot_state_publisher"
        type="robot_state_publisher"/>
</launch>
```

### 3.3 测试合并

**启动测试**：
```bash
# 终端 1：启动 can_driver
roslaunch can_driver can_driver.launch

# 终端 2：启动 canopen
roslaunch yiyou_canopen yiyou_canopen.launch

# 终端 3：启动合并节点
roslaunch robot_bringup joint_states_merge.launch

# 终端 4：检查合并结果
rostopic echo /joint_states
```

**检查���单**：
- [ ] /joint_states 包含 12 个关节
- [ ] 关节名称正确：arm_joint_1~6, left_track, right_track, flipper_1~4
- [ ] 数据实时更新
- [ ] TF 树完整（rosrun tf view_frames）

---

## 阶段四：arm_traj_splitter 代理节点（预计 1-2 天）⭐

### 4.1 创建包

```bash
cd ~/Robot24_catkin_ws/src
catkin_create_pkg arm_traj_splitter roscpp actionlib control_msgs trajectory_msgs
```

**检查清单**：
- [ ] 包已创建
- [ ] CMakeLists.txt 依赖正确
- [ ] package.xml 依赖正确

### 4.2 创建配置文件

**文件路径**：`~/Robot24_catkin_ws/src/arm_traj_splitter/config/splitter.yaml`

**内容**：
```yaml
arm_controller_name: "arm_controller"

backend_controllers:
  - name: "pp_arm_controller"
    action_ns: "/pp_arm_controller/follow_joint_trajectory"
    joints:
      - arm_joint_1
      - arm_joint_2
      - arm_joint_3
      - arm_joint_4

  - name: "canopen_arm_controller"
    action_ns: "/canopen/canopen_arm_controller/follow_joint_trajectory"
    joints:
      - arm_joint_5
      - arm_joint_6
```

### 4.3 实现核心代码

**文件结构**：
```
arm_traj_splitter/
├── include/arm_traj_splitter/
│   └── SplitterNode.h
├── src/
│   ├── SplitterNode.cpp
│   └── main.cpp
├── config/
│   └── splitter.yaml
└── launch/
    └── splitter.launch
```

**开发步骤**：

1. **SplitterNode.h**（~50 行）
   - [ ] 定义 SplitterNode 类
   - [ ] 声明 ActionServer 和 ActionClient
   - [ ] 声明 Backend 结构体
   - [ ] 声明核心方法：onGoal, splitTrajectory, splitTolerances

2. **SplitterNode.cpp**（~250 行）
   - [ ] 实现构造函数：加载参数、创建 server/clients
   - [ ] 实现 onGoal：接收 MoveIt 轨迹
   - [ ] 实现 splitTrajectory：按关节名拆分轨迹
   - [ ] 实现 splitTolerances：拆分容差参数
   - [ ] 实现轮询逻辑：等待两个后端完成
   - [ ] 实现 preempt 处理：取消所有后端
   - [ ] 实现错误处理：任一失败则全部取消

3. **main.cpp**（~20 行）
   - [ ] ROS 节点初始化
   - [ ] 创建 SplitterNode 实例
   - [ ] ros::spin()

4. **CMakeLists.txt**
   - [ ] 添加头文件路径
   - [ ] 添加可执行文件
   - [ ] 链接库

5. **splitter.launch**
   - [ ] 加载配置文件
   - [ ] 启动节点

### 4.4 编译测试

```bash
cd ~/Robot24_catkin_ws
catkin_make

# 检查编译结果
ls devel/lib/arm_traj_splitter/
```

**检查清单**：
- [ ] 编译无错误
- [ ] 可执行文件已生成

### 4.5 单元测试

**启动测试**：
```bash
# 终端 1：启动 can_driver
roslaunch can_driver can_driver.launch

# 终端 2：启动 canopen
roslaunch yiyou_canopen yiyou_canopen.launch

# 终端 3：启动 splitter
roslaunch arm_traj_splitter splitter.launch

# 终端 4：检查 action server
rostopic list | grep arm_controller
# 应该看到：/arm_controller/follow_joint_trajectory/...
```

**检查清单**：
- [ ] splitter 节点启动无错误
- [ ] 连接到两个后端 action server
- [ ] /arm_controller/follow_joint_trajectory action 可用

### 4.6 功能测试

**使用 actionlib 命令行工具测试**：
```bash
# 发送测试轨迹
rosrun actionlib_tools axclient.py /arm_controller/follow_joint_trajectory
```

**测试场景**：
- [ ] 发送 6 轴轨迹，观察两个后端是否同时执行
- [ ] 测试 preempt：执行中途取消
- [ ] 测试失败处理：手动停止一个后端，观察 splitter 反应
- [ ] 测试关节名错误：发送不存在的关节名

---

## 阶段五：MoveIt 配置（预计 4 小时）

### 5.1 运行 MoveIt Setup Assistant

```bash
roslaunch moveit_setup_assistant setup_assistant.launch
```

**配置步骤**：
1. [ ] Load URDF：加载机器人 URDF
2. [ ] Self-Collisions：生成碰撞矩阵
3. [ ] Planning Groups：
   - [ ] 创建 "arm" group，包含 arm_joint_1~6
   - [ ] 选择 kdl_kinematics_plugin
4. [ ] Robot Poses：定义常用姿态（home, stow 等）
5. [ ] End Effectors：定义末端执行器（如果有）
6. [ ] Passive Joints：标记被动关节（如果有）
7. [ ] Controllers：配置 arm_controller
8. [ ] Generate Package：生成配置包（如 robot_moveit_config）

### 5.2 修改 controllers.yaml

**文件路径**：`~/Robot24_catkin_ws/src/robot_moveit_config/config/controllers.yaml`

**修改为**：
```yaml
controller_list:
  - name: arm_controller
    action_ns: follow_joint_trajectory
    type: FollowJointTrajectory
    joints:
      - arm_joint_1
      - arm_joint_2
      - arm_joint_3
      - arm_joint_4
      - arm_joint_5
      - arm_joint_6
```

**检查清单**：
- [ ] controller name 为 arm_controller
- [ ] action_ns 为 follow_joint_trajectory（不带前缀）
- [ ] joints 列表包含全部 6 个关节

### 5.3 测试 MoveIt

**启动完整系统**：
```bash
# 终端 1：启动驱动层
roslaunch robot_bringup full_system.launch

# 终端 2：启动 MoveIt
roslaunch robot_moveit_config move_group.launch

# 终端 3：启动 RViz
roslaunch robot_moveit_config moveit_rviz.launch
```

**测试步骤**：
1. [ ] 在 RViz 中拖动交互式标记设置目标位姿
2. [ ] 点击 "Plan" 按钮，观察规划结果
3. [ ] 点击 "Execute" 按钮，观察真实机械臂运动
4. [ ] 测试多次规划和执行
5. [ ] 测试碰撞检测：添加障碍物

**检查清单**：
- [ ] MoveIt 能成功规划轨迹
- [ ] 轨迹能成功执行到真实机械臂
- [ ] 6 个关节协同运动
- [ ] 无明显延迟或抖动

---

## 阶段六：底盘与摆臂接入（预计 3 小时）

### 6.1 底盘参数测量

**任务**：测量实际底盘参数

**测量方法**：
- **wheel_separation**：用卷尺测量左右轮中心距（米）
- **wheel_radius**：测量驱动轮/履带等效半径（米）

**更新配置**：
```bash
# 编辑 ros_controllers.yaml
vim ~/Robot24_catkin_ws/src/can_driver/config/ros_controllers.yaml

# 修改 diff_drive_controller 部分
wheel_separation: 0.XX  # 实测值
wheel_radius: 0.XX      # 实测值
```

**检查清单**：
- [ ] 参数已测量
- [ ] 配置已更新

### 6.2 底盘测试

**测试命令**：
```bash
# 启动系统
roslaunch robot_bringup full_system.launch

# 发送速度命令
rostopic pub /cmd_vel geometry_msgs/Twist \
  "linear: {x: 0.1, y: 0, z: 0}
   angular: {x: 0, y: 0, z: 0}" -r 10

# 观察底盘运动

# 检查里程计
rostopic echo /diff_drive_controller/odom
```

**测试场景**：
- [ ] 前进：linear.x = 0.1
- [ ] 后退：linear.x = -0.1
- [ ] 左转：angular.z = 0.5
- [ ] 右转：angular.z = -0.5
- [ ] 停止：全部为 0

**检查清单**：
- [ ] 底盘响应速度命令
- [ ] 运动方向正确
- [ ] 里程计数据合理

### 6.3 摆臂测试

**测试命令**：
```bash
# 发送摆臂位置命令
rostopic pub /flipper_controller/command std_msgs/Float64MultiArray \
  "data: [0.1, 0.1, -0.1, -0.1]"

# 观察摆臂运动
```

**测试场景**：
- [ ] 全部抬起：[0.5, 0.5, 0.5, 0.5]
- [ ] 全部放下：[0, 0, 0, 0]
- [ ] 前后不同：[0.5, 0.5, 0, 0]
- [ ] 左右不同：[0.5, 0, 0.5, 0]

**检查清单**：
- [ ] 摆臂响应位置命令
- [ ] 4 个摆臂独立控制
- [ ] 运动平滑无抖动

---

## 阶段七：顶层 Launch 整合（预计 2 小时）

### 7.1 创建 full_system.launch

**文件路径**：`~/Robot24_catkin_ws/src/robot_bringup/launch/full_system.launch`

**内容**：
```xml
<launch>
  <!-- URDF -->
  <param name="robot_description"
         command="$(find xacro)/xacro
                  '$(find robot_description)/urdf/robot.urdf.xacro'"/>

  <!-- 驱动层 -->
  <include file="$(find can_driver)/launch/can_driver.launch"/>
  <include file="$(find yiyou_canopen)/launch/yiyou_canopen.launch"/>

  <!-- 状态合并 -->
  <node name="joint_state_publisher"
        pkg="joint_state_publisher"
        type="joint_state_publisher">
    <rosparam param="source_list">
      - can_driver/joint_states
      - canopen/joint_states
    </rosparam>
  </node>

  <node name="robot_state_publisher"
        pkg="robot_state_publisher"
        type="robot_state_publisher"/>

  <!-- 机械臂轨迹代理 -->
  <include file="$(find arm_traj_splitter)/launch/splitter.launch"/>
</launch>
```

### 7.2 创建启动脚本

**文件路径**：`~/Robot24_catkin_ws/start_robot.sh`

**内容**：
```bash
#!/bin/bash

# CAN 接口初始化
sudo ip link set can0 up type can bitrate 1000000
sudo ip link set can1 up type can bitrate 1000000

# 检查 CAN 接口状态
ip -details link show can0
ip -details link show can1

# 启动系统
roslaunch robot_bringup full_system.launch
```

**设置权限**：
```bash
chmod +x ~/Robot24_catkin_ws/start_robot.sh
```

### 7.3 完整系统测试

**启动测试**：
```bash
# 一键启动
~/Robot24_catkin_ws/start_robot.sh
```

**检查清单**：
- [ ] 所有节点启动无错误
- [ ] /joint_states 包含 12 个关节
- [ ] TF 树完整
- [ ] 所有 action servers 可用
- [ ] 所有 controllers 运行中

---

## 总体验收清单

### 功能验收

- [ ] **机械臂**：MoveIt 能规划并执行 6 轴轨迹
- [ ] **底盘**：响应 /cmd_vel 命令，里程计正常
- [ ] **摆臂**：响应位置命令，4 个摆臂独立控制
- [ ] **状态反馈**：12 个关节状态实时发布
- [ ] **TF 树**：完整且正确

### 性能验收

- [ ] 控制频率：100 Hz 稳定运行
- [ ] 轨迹延迟：< 50ms
- [ ] 规划成功率：> 90%
- [ ] 连续运行：> 30 分钟无异常

### 文档验收

- [ ] 启动文档：一键启动脚本
- [ ] 配置文档：参数说明
- [ ] 故障排查：常见问题解决方案
- [ ] 接口文档：topic/service/action 列表

---

## 时间估算

| 阶段 | 预计时间 | 关键风险 |
|------|---------|---------|
| 阶段一：URDF | 4h | URDF 可能不存在，需要从头创建 |
| 阶段二：Controller | 2h | 命名空间配置容易出错 |
| 阶段三：合并 | 1h | 低风险 |
| 阶段四：Splitter | 16h | 核心开发，需要仔细测试 |
| 阶段五：MoveIt | 4h | 运动学配置可能需要调试 |
| 阶段六：底盘摆臂 | 3h | 参数测量需要硬件配合 |
| 阶段七：整合 | 2h | 低风险 |
| **总计** | **32h (4天)** | |

---

## 下一步行动

建议按以下顺序执行：

1. **立即开始**：阶段一 URDF 验证（查找或创建）
2. **并行准备**：阶段二 Controller 配置（创建 canopen controllers.yaml）
3. **快速验证**：阶段三 joint_states 合并
4. **核心开发**：阶段四 arm_traj_splitter（预留充足时间）
5. **集成测试**：阶段五~七
